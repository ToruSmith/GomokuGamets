{
  "rules": {
    "rooms": {
      "$roomCode": {
        ".read": "auth != null",
        ".write": "auth != null",
        "board": {
          "$row": {
            "$col": {
              ".write": "auth != null && !data.exists()"
            }
          }
        }
      }
    }
  }
}
